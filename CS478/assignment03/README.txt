Packages Required
Python
PyShark

To use:
Install the above required packages. Save the packet capture data to be inspected to the save directory as the stallkaj_Assignment03.py file and name the packet data file "win.cap". Run the python script.

Output will be displayed in console prompt. An example cap has been included.